#summary The document.bgColor property
#labels about-dom,is-dom-property

== Values ==

The `document.bgColor` property returns a string.

This property is read/write.

It supports both English names of colors and Hexadecimal values. 

That means we can assign values in string format (red, blue etc.) or in hexadecimal format(#fdecb2).

== Usage ==

== Browser compatibility ==

[ArticleBrowserCompatibilityLegend Compatibility table legend]

|| *Test* || *IE8* || *IE7* || *IE6* || *FF3* || *FF2* || *Saf3* || *Op9* || *Chrome* ||
|| [http://doctype.googlecode.com/svn/trunk/tests/js/document/document-bgColor-typeof-test.html typeOf(document.bgColor) != 'undefined'] || Y || Y || Y || Y || Y || Y || Y || Y ||

== Further reading ==

  * [http://developer.mozilla.org/en/docs/DOM:document.bgColor The document.bgColor property on Mozilla Developer Center]
  * [http://msdn2.microsoft.com/en-us/library/ms533506.aspx The document.bgColor property on MSDN]

