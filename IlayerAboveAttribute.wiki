#summary The ilayer above attribute
#labels about-html,is-html-attribute

You are here: [Welcome Home] > [HTMLReference HTML Reference] > [HTMLElements Elements] > [IlayerElement <ilayer> element] > *above attribute*

== Usage ==

Do not use [ILayerElement <ilayer>].  The `ilayer` element was introduced in [BrowserNetscape4 Netscape 4] and has never been supported by any other browser.

== Elements ==

You can use the `above` attribute on the following elements:

  * [IlayerElement <ilayer>]
  * [LayerElement <layer>]

== Browser compatibility ==

[ArticleBrowserCompatibilityLegend Compatibility table legend]

|| *Test* || *IE8* || *IE7* || *IE6* || *FF3* || *FF2* || *Saf3* || *Op9* || *Chrome* ||
|| [http://doctype.googlecode.com/svn/trunk/tests/html/attributes/ilayer-above-reflection-test.html ilayer.above reflects <ilayer above="foo">] || Y || Y || Y || N || N || N || Y || N ||

== Further reading ==


