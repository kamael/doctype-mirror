#summary The document.scripts property
#labels about-dom,is-dom-property

== Values ==

The `document.scripts` property returns an array of [HTMLScriptElementObject HTMLScriptElement object]s.

This property is read-only.

== Usage ==

== Browser compatibility ==

[ArticleBrowserCompatibilityLegend Compatibility table legend]

|| *Test* || *IE8* || *IE7* || *IE6* || *FF3* || *FF2* || *Saf3* || *Op9* || *Chrome* ||
|| [http://doctype.googlecode.com/svn/trunk/tests/js/document/document-scripts-typeof-test.html typeOf(document.scripts) != 'undefined'] || N || N || N || N || N || Y || Y || Y ||

== Further reading ==

  * [http://msdn2.microsoft.com/en-us/library/ms537487.aspx The document.scripts property on MSDN]

